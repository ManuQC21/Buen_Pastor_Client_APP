<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="Buen.Pastor.app.Activity.ui.docente.NotificacionesDocentesFragment">

    <!-- RecyclerView para la lista de notificaciones -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_notificaciones"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        tools:listitem="@layout/item_notificaciones" />

    <!-- Texto cuando no hay notificaciones -->
    <TextView
        android:id="@+id/no_notificaciones_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No tiene Notificaciones"
        android:textSize="20sp"
        android:textColor="@color/azul_general"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_gravity="center"
        android:visibility="gone" />
</FrameLayout>
